{
  "name": "kds-airdrop-hub",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "wrangler": "^4.131.2"
  }
}
